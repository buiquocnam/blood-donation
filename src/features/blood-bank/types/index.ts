/**
 * Types for Blood Bank feature
 * Uses global types from src/types/index.ts
 */

import type { BloodTypeStats, DonationStats, BloodBankReport } from '@/types/reports';

// Re-export necessary types từ global types
export type { 
  BloodTypeStats, 
  DonationStats, 
  BloodBankReport 
}; 